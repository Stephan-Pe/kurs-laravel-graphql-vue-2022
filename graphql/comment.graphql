type Comment {
    id: ID!
    title: String!
    nickname: String!
    body: String
    post_id: Int
    post: Post
    created_at: DateTime!
    updated_at: DateTime!
}

extend type Query {
    comments: [Comment!]! @all
}